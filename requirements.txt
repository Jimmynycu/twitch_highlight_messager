aiohttp>=3.9
pywebview>=5.0          # native desktop window (no browser)
httpx>=0.24            # used by the vendored ai_sub_auth (ChatGPT-subscription brains)
# openai>=1.0          # optional: use an OpenAI API key instead of the subscription
# ai_sub_auth is vendored in-repo (its own PyPI packaging is broken) — see ai_sub_auth/LICENSE
